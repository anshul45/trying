import { Modal } from '@mantine/core'
import React from 'react'

const CreateModal = () => {
  return (
    <Modal opened={true} onClose={() => {}} centered>

    </Modal>
  )
}

export default CreateModal